---

volumes:
  tmp:
    # -- tmp volume size limit
    sizeLimit: "128Mi"
    # -- tmp volume mount path
    mountPath: "/tmp"
  logs:
    # -- logs volume size limit
    sizeLimit: "128Mi"
    # -- logs volume mount path
    mountPath: "/logs"
